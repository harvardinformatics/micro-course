#!/bin/bash

if [[ -z "$@" ]]; then
    echo 'You must supply a fastq file (e.g. megaAssembler human.fastq)'
    exit 1
fi
echo "Assembling genome in $1"
sleep 10
echo 'Out of memory error' >&2
exit 1
